const assert=require('node:assert/strict'),ts=require('typescript'),fs=require('fs'),Module=require('module');const path=require('path').resolve('src/features/learn/catalog/structureCatalog.ts');const m=new Module(path,module);m._compile(ts.transpileModule(fs.readFileSync(path,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText,path);const {structures,browseStructures,recommendedStructure,topicPercent}=m.exports;
assert.equal(structures.length,10);assert.equal(recommendedStructure(null).id,'array');const p={topics:[{topicId:'array',status:'completed',completionPercent:100},{topicId:'linked-list',status:'in-progress',completionPercent:75}],recommendedTopics:[],weakAreas:[]};assert.equal(recommendedStructure(p).id,'linked-list');assert.equal(topicPercent(structures[1],p),75);assert.equal(browseStructures('','All','Progress',p)[0].id,'array');assert.equal(browseStructures('hashmap','All','Popular',null)[0].id,'hash-table');assert.equal(browseStructures('priority queue','All','Popular',null)[0].id,'heap');assert.equal(browseStructures('','Non-Linear','Popular',null).length,5);assert.equal(browseStructures('','Advanced','Popular',null).length,3);assert.deepEqual(browseStructures('','All','Popular',null).map(x=>x.id),structures.map(x=>x.id));assert.equal(browseStructures('no such topic','All','Popular',null).length,0);console.log('PASS catalog aliases, categories, stable order, progress sorting and next incomplete topic');
