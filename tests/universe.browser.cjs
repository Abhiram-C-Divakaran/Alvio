const assert=require('node:assert/strict');const {chromium}=require('C:/Users/Abhiram/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1536,height:1024}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:3001/learn');await p.locator('canvas').waitFor();await p.waitForTimeout(2200);await p.screenshot({path:'data/universe-desktop.png'});await p.getByRole('button',{name:'Focus Arrays',exact:true}).click();await p.locator('.uv-details h2').filter({hasText:'Arrays'}).waitFor();await p.getByRole('button',{name:'Reset View',exact:true}).click();await p.waitForTimeout(500);assert.equal(await p.locator('.uv-details h2').innerText(),'Arrays');await p.getByRole('button',{name:'Focus Stacks',exact:true}).focus();await p.keyboard.press('Enter');assert.equal(await p.locator('.uv-details h2').innerText(),'Stacks');await p.getByRole('button',{name:'List',exact:true}).click();await p.getByLabel('Search data structures').fill('pointer');assert.equal(await p.locator('.uv-list>button').count(),1);await p.getByLabel('Search data structures').fill('');await p.getByRole('button',{name:'Linear',exact:true}).click();assert.equal(await p.locator('.uv-list>button').count(),4);await p.getByRole('button',{name:'Hashing',exact:true}).click();assert.equal(await p.locator('.uv-list>button').count(),1);await p.locator('.uv-list>button').click();assert.equal(await p.locator('.uv-primary').getAttribute('href'),'/learn/hash-table');await p.keyboard.press('Escape');assert.match(await p.locator('.uv-details').innerText(),/Select a topic/);await p.setViewportSize({width:390,height:844});await p.reload();await p.locator('.uv-list').waitFor();assert.equal(await p.locator('canvas').count(),0);assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await p.screenshot({path:'data/universe-mobile.png',fullPage:true,animations:'disabled'});assert.deepEqual(errors,[]);await b.close();console.log('PASS map selection, list filters, search, lesson links, Escape and mobile default');})().catch(e=>{console.error(e);process.exit(1)});
