export const languages=['Python','C++','Java','JavaScript'] as const;
export type ExampleLanguage=typeof languages[number];
const python:Record<string,string>={arrays:'numbers = [10, 20, 30, 40, 50]\nprint(numbers[2])  # 30\nnumbers[2] = 35', 'linked-lists':'class Node:\n    def __init__(self, value, next=None):\n        self.value, self.next = value, next\nhead = Node(10, Node(20))\nprint(head.next.value)  # 20',queues:'from collections import deque\nqueue = deque([10, 20])\nqueue.append(30)\nprint(queue.popleft())  # 10','binary-tree':'class Node:\n    def __init__(self, value, left=None, right=None):\n        self.value, self.left, self.right = value, left, right\nroot = Node(20, Node(10), Node(30))\nprint(root.left.value)  # 10',hash:'scores = {"Alice": 85, "Bob": 92}\nscores["Alice"] = 90\nprint(scores["Alice"])  # 90',heap:'import heapq\nheap = [30, 10, 20]\nheapq.heapify(heap)\nheapq.heappush(heap, 5)\nprint(heapq.heappop(heap))  # 5'};
const js:Record<string,string>={arrays:'const numbers = [10, 20, 30, 40, 50];\nconsole.log(numbers[2]); // 30\nnumbers[2] = 35;', 'linked-lists':'const head = {value: 10, next: {value: 20, next: null}};\nconsole.log(head.next.value); // 20',queues:'const queue = [10, 20];\nqueue.push(30);\nconsole.log(queue.shift()); // 10\n// shift() is O(n); use a head index for larger queues.','binary-tree':'const root = {value: 20, left: {value: 10}, right: {value: 30}};\nconsole.log(root.left.value); // 10',hash:'const scores = new Map([["Alice", 85], ["Bob", 92]]);\nscores.set("Alice", 90);\nconsole.log(scores.get("Alice")); // 90',heap:'// Sift up a new value in an existing min-heap.\nconst heap = [10, 30, 20];\nheap.push(5);\nlet i = heap.length - 1;\nwhile (i > 0) {\n  const p = Math.floor((i - 1) / 2);\n  if (heap[p] <= heap[i]) break;\n  [heap[p], heap[i]] = [heap[i], heap[p]];\n  i = p;\n}\nconsole.log(heap[0]); // 5'};
const cpp:Record<string,string>={arrays:'std::vector<int> numbers{10,20,30,40,50};\nstd::cout << numbers.at(2); // 30', 'linked-lists':'std::forward_list<int> nodes{10,20};\nstd::cout << *std::next(nodes.begin()); // 20',queues:'std::queue<int> q; q.push(10); q.push(20);\nstd::cout << q.front(); q.pop(); // 10','binary-tree':'struct Node { int value; Node *left, *right; };\nNode left{10,nullptr,nullptr}, right{30,nullptr,nullptr};\nNode root{20,&left,&right};\nstd::cout << root.left->value;',hash:'std::unordered_map<std::string,int> scores{{"Alice",85}};\nscores["Alice"] = 90;\nstd::cout << scores.at("Alice");',heap:'std::priority_queue<int,std::vector<int>,std::greater<int>> heap;\nheap.push(30); heap.push(10); heap.push(5);\nstd::cout << heap.top(); heap.pop(); // 5'};
const java:Record<string,string>={arrays:'int[] numbers = {10,20,30,40,50};\nSystem.out.println(numbers[2]); // 30', 'linked-lists':'LinkedList<Integer> nodes = new LinkedList<>(List.of(10,20));\nSystem.out.println(nodes.getLast()); // 20',queues:'Queue<Integer> q = new ArrayDeque<>(List.of(10,20));\nq.add(30);\nSystem.out.println(q.remove()); // 10','binary-tree':'class Node { int value; Node left, right; Node(int v){value=v;} }\nNode root=new Node(20); root.left=new Node(10); root.right=new Node(30);\nSystem.out.println(root.left.value);',hash:'Map<String,Integer> scores=new HashMap<>();\nscores.put("Alice",90);\nSystem.out.println(scores.get("Alice"));',heap:'PriorityQueue<Integer> heap=new PriorityQueue<>(List.of(30,10,20));\nheap.add(5);\nSystem.out.println(heap.remove()); // 5'};
export function codeExample(id:string,language:ExampleLanguage){if(language==='Python')return python[id];if(language==='JavaScript')return js[id];if(language==='C++')return '#include <iostream>\n#include <vector>\n#include <queue>\n#include <forward_list>\n#include <iterator>\n#include <functional>\n#include <unordered_map>\n#include <string>\nint main() {\n'+cpp[id]+'\n}';return 'import java.util.*;\nclass Main {\n  public static void main(String[] args) {\n'+java[id]+'\n  }\n}'}
